{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts.yumpoo.local/events/catalog.product-owner-reassigned-v1.schema.json",
  "title": "Product owner reassigned",
  "allOf": [
    { "$ref": "event-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "catalog.product_owner_reassigned" },
        "eventVersion": { "const": 1 },
        "aggregateType": { "const": "Product" },
        "payload": {
          "type": "object", "additionalProperties": false,
          "required": ["productId", "code", "name", "status", "ownerUserId", "previousOwnerUserId", "newOwnerUserId"],
          "properties": {
            "productId": { "type": "string", "format": "uuid" }, "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]{1,31}$" },
            "name": { "type": "string", "minLength": 1, "maxLength": 80 }, "status": { "enum": ["ACTIVE", "ARCHIVED"] }, "ownerUserId": { "type": "string", "format": "uuid" },
            "previousOwnerUserId": { "type": "string", "format": "uuid" }, "newOwnerUserId": { "type": "string", "format": "uuid" }
          }
        }
      }
    }
  ]
}
