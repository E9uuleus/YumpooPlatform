{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://contracts.yumpoo.local/events/catalog.project-activated-v1.schema.json",
  "title": "Project activated",
  "allOf": [
    { "$ref": "event-envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "catalog.project_activated" },
        "eventVersion": { "const": 1 },
        "aggregateType": { "const": "Project" },
        "aggregateVersion": { "type": "integer", "minimum": 1 },
        "payload": {
          "type": "object",
          "additionalProperties": false,
          "required": ["projectId", "code", "fromLifecycle", "toLifecycle", "ownerUserId", "templateKey", "templateVersion", "rowVersion"],
          "properties": {
            "projectId": { "type": "string", "format": "uuid" },
            "code": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]{1,31}$" },
            "fromLifecycle": { "const": "DRAFT" },
            "toLifecycle": { "const": "ACTIVE" },
            "ownerUserId": { "type": "string", "format": "uuid" },
            "templateKey": { "enum": ["RND", "PRE_SALES", "IMPLEMENTATION", "HYPERCARE"] },
            "templateVersion": { "type": "integer", "minimum": 1 },
            "rowVersion": { "type": "integer", "minimum": 1 }
          }
        }
      }
    }
  ]
}
