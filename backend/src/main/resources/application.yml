spring:
  application:
    name: yumpoo-server

server:
  address: 127.0.0.1
  port: ${YUMPOO_SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
